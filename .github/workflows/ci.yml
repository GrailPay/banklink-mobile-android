name: CI

on:
  push:
    branches: [master, develop]
  pull_request:

# Cancel in-progress runs for the same ref when a new commit is pushed.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-and-lint:
    name: Unit tests & Android Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # AGP 8.x / Gradle 8.13 must run on JDK 17 (the project still targets Java 11 bytecode).
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      # Unit tests for every module (:bank-link, :example) plus Android Lint.
      # connectedAndroidTest (example/src/androidTest) needs an emulator and is intentionally
      # left out of this fast PR check.
      - name: Run unit tests and lint
        run: ./gradlew --no-daemon --stacktrace test lint

      - name: Upload test & lint reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reports
          path: |
            **/build/reports/
            **/build/test-results/
          if-no-files-found: ignore
          retention-days: 14
